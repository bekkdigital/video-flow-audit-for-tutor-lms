# Composer dev tooling only — the runtime dependency (Video Flow Core) is
# bundled in lib/ and committed. Refresh it with `composer run sync-core`.
/vendor/
/composer.lock
/composer.phar
/.phpunit.result.cache
/dist/

# Internal dev-agent notes — kept local, not published.
/CLAUDE.md
/AGENTS.md
